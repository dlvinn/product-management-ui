interface Retailer {
  id: number; // Unique identifier for the retailer
  name: string; // Retailer name (e.g., "Retailer A")
  location: string; // Location of the retailer (e.g., "City, Country")
  contactInfo?: string; // Contact information (e.g., "info@retailera.com")
}
